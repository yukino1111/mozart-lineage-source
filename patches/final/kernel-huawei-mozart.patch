diff --git a/arch/arm64/configs/lineage_mozart_defconfig b/arch/arm64/configs/lineage_mozart_defconfig
index 468325fd043cfc044106fbf9e3b4f2853f118107..82ab3dc616c6ab3caacad934b7e06bb6d627128b 100755
--- a/arch/arm64/configs/lineage_mozart_defconfig
+++ b/arch/arm64/configs/lineage_mozart_defconfig
@@ -180,7 +180,7 @@ CONFIG_MAGIC_SYSRQ=y
 CONFIG_DEBUG_FS=y
 CONFIG_DEBUG_KERNEL=y
 # CONFIG_SCHED_DEBUG is not set
-CONFIG_DEBUG_INFO=y
+# CONFIG_DEBUG_INFO is not set
 CONFIG_ATOMIC64_SELFTEST=y
 CONFIG_PRINTK_TIME=y
 CONFIG_HISI_TIME=y
diff --git a/arch/arm64/kernel/ptrace.c b/arch/arm64/kernel/ptrace.c
index 13443a10e320b6c893256dc44910b60cbf4fb3ad..38de64949712ddb126ae3bb14f677983984d7d63 100755
--- a/arch/arm64/kernel/ptrace.c
+++ b/arch/arm64/kernel/ptrace.c
@@ -552,6 +552,33 @@ static int tls_set(struct task_struct *target, const struct user_regset *regset,
 	return ret;
 }
 
+static int system_call_get(struct task_struct *target,
+			   const struct user_regset *regset,
+			   unsigned int pos, unsigned int count,
+			   void *kbuf, void __user *ubuf)
+{
+	int syscallno = task_pt_regs(target)->syscallno;
+
+	return user_regset_copyout(&pos, &count, &kbuf, &ubuf,
+				   &syscallno, 0, -1);
+}
+
+static int system_call_set(struct task_struct *target,
+			   const struct user_regset *regset,
+			   unsigned int pos, unsigned int count,
+			   const void *kbuf, const void __user *ubuf)
+{
+	int syscallno, ret;
+
+	ret = user_regset_copyin(&pos, &count, &kbuf, &ubuf,
+				 &syscallno, 0, -1);
+	if (ret)
+		return ret;
+
+	task_pt_regs(target)->syscallno = syscallno;
+	return ret;
+}
+
 enum aarch64_regset {
 	REGSET_GPR,
 	REGSET_FPR,
@@ -560,6 +587,7 @@ enum aarch64_regset {
 	REGSET_HW_BREAK,
 	REGSET_HW_WATCH,
 #endif
+	REGSET_SYSTEM_CALL,
 };
 
 static const struct user_regset aarch64_regsets[] = {
@@ -609,6 +637,14 @@ static const struct user_regset aarch64_regsets[] = {
 		.set = hw_break_set,
 	},
 #endif
+	[REGSET_SYSTEM_CALL] = {
+		.core_note_type = NT_ARM_SYSTEM_CALL,
+		.n = 1,
+		.size = sizeof(int),
+		.align = sizeof(int),
+		.get = system_call_get,
+		.set = system_call_set,
+	},
 };
 
 static const struct user_regset_view user_aarch64_view = {
diff --git a/drivers/gpu/arm/Kconfig b/drivers/gpu/arm/Kconfig
index f4a46cbe01d4c261fe4c2c9a73740bfe941870e1..c5ab45fadf0ed8f6a2362f60569170ad43f2d66d 100755
--- a/drivers/gpu/arm/Kconfig
+++ b/drivers/gpu/arm/Kconfig
@@ -8,7 +8,7 @@ source "drivers/gpu/arm/mali4_64/Kconfig"
 endif
 
 if HISI_3635
-source "drivers/gpu/arm/malit6_64/Kconfig"
+source "drivers/gpu/arm/r8p0-01dev0/Kconfig"
 endif
 
 if ARCH_HI3630
diff --git a/drivers/gpu/arm/Makefile b/drivers/gpu/arm/Makefile
index 966ad819cf052e0b54fc2551b4c027dd0bf21c9d..8786f2e218e33af2273a25b9bb91019b4a842dc5 100755
--- a/drivers/gpu/arm/Makefile
+++ b/drivers/gpu/arm/Makefile
@@ -7,7 +7,7 @@ endif
 endif
 
 ifdef CONFIG_HISI_3635
-obj-y                   += malit6_64/
+obj-y                   += r8p0-01dev0/
 endif
 
 ifdef CONFIG_ARCH_HI3630
diff --git a/include/uapi/linux/elf.h b/include/uapi/linux/elf.h
index ef6103bf1f9bb3ade18268a639cc784dcc695b95..80c61675815f7d45ab6108c0e873fae4131a943b 100644
--- a/include/uapi/linux/elf.h
+++ b/include/uapi/linux/elf.h
@@ -395,6 +395,7 @@ typedef struct elf64_shdr {
 #define NT_ARM_TLS	0x401		/* ARM TLS register */
 #define NT_ARM_HW_BREAK	0x402		/* ARM hardware breakpoint registers */
 #define NT_ARM_HW_WATCH	0x403		/* ARM hardware watchpoint registers */
+#define NT_ARM_SYSTEM_CALL	0x404	/* ARM system call number */
 #define NT_METAG_CBUF	0x500		/* Metag catch buffer registers */
 #define NT_METAG_RPIPE	0x501		/* Metag read pipeline state */
 #define NT_METAG_TLS	0x502		/* Metag TLS pointer */
